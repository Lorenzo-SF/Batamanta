name: CI - Smoke Tests Matrix

on:
  push:
    branches: [ main, develop ]
    tags: [ 'v*' ]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

env:
  # Edge versions for testing
  ELIXIR_OLD: '1.15.8'
  ELIXIR_NEW: '1.18.4'
  OTP_OLD: '25.3.2'
  OTP_NEW: '28.1'

jobs:
  # ============================================================================
  # UNIT TESTS - Edge versions
  # ============================================================================
  unit_tests:
    name: Unit Tests (Elixir ${{ matrix.elixir }}, OTP ${{ matrix.otp }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - elixir: ${{ env.ELIXIR_OLD }}
            otp: ${{ env.OTP_OLD }}
          - elixir: ${{ env.ELIXIR_NEW }}
            otp: ${{ env.OTP_NEW }}

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Erlang/OTP and Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ matrix.elixir }}
          otp-version: ${{ matrix.otp }}
          install-hex: true
          install-rebar: true

      - name: Install System Dependencies
        run: sudo apt-get update && sudo apt-get install -y zstd

      - name: Install Mix Dependencies
        run: mix deps.get

      - name: Run Tests (ExUnit)
        run: mix test

      - name: Run Credo (Code Quality)
        run: mix credo --strict

  # ============================================================================
  # SMOKE TESTS - Linux AMD64 glibc
  # ============================================================================
  smoke_tests_linux_amd64_glibc:
    name: Smoke Tests Linux AMD64 glibc (${{ matrix.mode }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        mode: [cli, tui, daemon]
        include:
          - elixir: ${{ env.ELIXIR_NEW }}
            otp: ${{ env.OTP_NEW }}

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Erlang/OTP and Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ matrix.elixir }}
          otp-version: ${{ matrix.otp }}
          install-hex: true
          install-rebar: true

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu

      - name: Install System Dependencies
        run: sudo apt-get update && sudo apt-get install -y zstd

      - name: Install Mix Dependencies
        working-directory: smoke_tests/test_${{ matrix.mode }}
        run: mix deps.get

      - name: Build with Batamanta
        working-directory: smoke_tests/test_${{ matrix.mode }}
        run: mix batamanta --compression 1

      - name: Run Smoke Test
        run: |
          chmod +x smoke_test_runner.sh
          ./smoke_test_runner.sh smoke_tests/test_${{ matrix.mode }} ${{ matrix.mode }} 30

  # ============================================================================
  # SMOKE TESTS - Linux AMD64 musl (Alpine via Docker)
  # ============================================================================
  smoke_tests_linux_amd64_musl:
    name: Smoke Tests Linux AMD64 musl
    runs-on: ubuntu-latest
    container:
      image: elixir:1.18.4-otp-28-alpine

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install System Dependencies (Alpine)
        run: apk add --no-cache build-base rust cargo zstd

      - name: Install Mix Dependencies
        working-directory: smoke_tests/test_cli
        run: mix local.hex --force && mix local.rebar --force && mix deps.get

      - name: Build with Batamanta
        working-directory: smoke_tests/test_cli
        run: mix batamanta --compression 1

      - name: Run Smoke Test
        run: |
          chmod +x smoke_test_runner.sh
          ./smoke_test_runner.sh smoke_tests/test_cli cli 30

  # ============================================================================
  # DOCKER MATRIX - Full coverage
  # ============================================================================
  docker_matrix:
    name: Docker Matrix (${{ matrix.image }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - image: elixir:${{ env.ELIXIR_OLD }}-otp-${{ env.OTP_OLD }}
          - image: elixir:${{ env.ELIXIR_NEW }}-otp-${{ env.OTP_NEW }}

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Run Docker Test Matrix
        run: |
          chmod +x docker_matrix_simple.sh
          ./docker_matrix_simple.sh ${{ matrix.image }}

  # ============================================================================
  # COVERAGE REPORT
  # ============================================================================
  coverage:
    name: Coverage Report
    runs-on: ubuntu-latest
    needs: [unit_tests]

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Erlang/OTP and Elixir
        uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ env.ELIXIR_NEW }}
          otp-version: ${{ env.OTP_NEW }}
          install-hex: true
          install-rebar: true

      - name: Install System Dependencies
        run: sudo apt-get update && sudo apt-get install -y zstd

      - name: Install Mix Dependencies
        run: mix deps.get

      - name: Run Tests with Coverage
        run: mix test --cover

      - name: Upload Coverage Report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: cover/
          retention-days: 30

  # ============================================================================
  # RELEASE (only on tags)
  # ============================================================================
  release:
    name: Publish to Hex.pm
    needs: [unit_tests, smoke_tests_linux_amd64_glibc, smoke_tests_linux_amd64_musl, docker_matrix]
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
      - uses: actions/checkout@v4
      
      - uses: erlef/setup-beam@v1
        with:
          elixir-version: ${{ env.ELIXIR_NEW }}
          otp-version: ${{ env.OTP_NEW }}
      
      - run: mix deps.get
      - run: mix compile
      - run: mix hex.build
      - run: mix hex.publish --yes
        env:
          HEX_API_KEY: ${{ secrets.HEX_API_KEY }}
